import 'package:flutter/material.dart';

/// สีหลักที่ใช้ซ้ำทั้งแอป
class AppColors {
  AppColors._();
  static const Color bg = Color(0xFF0B0F19);      // พื้นหลังเข้ม
  static const Color bgsecondary = Color(0xFF111827);  // พื้นหลังการ์ดข้างใน
  static const Color onBg = Colors.white;         // ตัวอักษร/ไอคอนบนพื้นหลังเข้ม
  static const Color primary = Color(0xFF16A34A); // เขียวไฮไลต์ (tab ที่เลือก)
  static const Color outline = Color(0xFFBBB9B9); // เส้น/เงาบางๆ
}